// Backward-compatible student avatar. Now delegates to the shared PersonAvatar,
// which also accepts an optional photo URL for display.
export { PersonAvatar as StudentAvatar } from "@/components/shared/PersonAvatar"